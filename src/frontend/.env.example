# Backend API Base URL (defaults to http://<current_hostname>:5000 if omitted)
# Useful for overriding the backend URL during development or local network sharing
VITE_API_BASE_URL=http://localhost:5000
